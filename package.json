{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --watch",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.4.1",
    "nuxt": "^3.2.2"
  },
  "dependencies": {
    "watch": "^1.0.2"
  }
}
